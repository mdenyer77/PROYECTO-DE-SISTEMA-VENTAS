# ?? GUÍA RÁPIDA - ARREGLAR ERROR DE VENTAS

## El problema
```
Form3 abre
  ?
Error: "Error al cargar las ventas: el nombre de objeto ventas no es válido"
  ?
DataGrid vacío
```

## La causa
El procedimiento almacenado `sp_ListarVentasConCliente` **NO EXISTE** en tu base de datos.

## La solución (3 pasos simples)

### PASO 1: Abre SQL Server Management Studio
- Click derecho en tu base de datos
- Click en "Nueva consulta"

### PASO 2: Copia este SQL
```sql
IF EXISTS (SELECT 1 FROM sys.objects WHERE type = 'P' AND name = 'sp_ListarVentasConCliente')
    DROP PROCEDURE sp_ListarVentasConCliente
GO

CREATE PROCEDURE sp_ListarVentasConCliente
AS
BEGIN
    SELECT 
        v.ID_venta,
        v.Fecha_venta,
        c.ID_cliente,
        c.Nombre AS Cliente_Nombre,
        c.Telefono AS Cliente_Telefono,
        c.Direccion AS Cliente_Direccion,
        v.Total_general,
        CASE WHEN v.Estado_venta = 1 THEN 'Activa' ELSE 'Cancelada' END AS Estado_venta
    FROM Ventas v
    INNER JOIN Cliente c ON v.ID_cliente = c.ID_cliente
    ORDER BY v.Fecha_venta DESC
END
GO
```

### PASO 3: Ejecuta (F5)
- Pega el código
- Presiona F5
- Deberías ver: "Command(s) completed successfully"

## Después

1. Cierra SQL Server
2. Abre tu aplicación
3. Abre Form3
4. **¡Deberías ver todas tus ventas!**

---

## Si aún no funciona

Ejecuta esto en SQL para crear TODOS los procedimientos necesarios:

**Archivo**: `SQL_TODOS_PROCEDIMIENTOS_COMPLETOS.sql`

Cópialo completo y ejecuta en SQL Server Management Studio.

---

## ? Listo!

Ese es todo. 2 minutos y listo.

Si necesitas más procedimientos, revisa el archivo:
**SQL_TODOS_PROCEDIMIENTOS_COMPLETOS.sql**
