# ? LA TABLA SE LLAMA "venta" (SINGULAR)

## ?? Lo que necesitas hacer

### PASO 1: Abre SQL Server Management Studio

### PASO 2: Ejecuta este SQL en tu BD:

```sql
IF EXISTS (SELECT 1 FROM sys.objects WHERE type = 'P' AND name = 'sp_listar_venta')
    DROP PROCEDURE sp_listar_venta
GO

CREATE PROCEDURE sp_listar_venta
AS
BEGIN
    SELECT 
        ID_venta,
        Fecha_venta,
        ID_cliente,
        Total_general,
        Estado_venta
    FROM venta
    ORDER BY Fecha_venta DESC
END
GO
```

### PASO 3: Verifica que funciona:
```sql
EXEC sp_listar_venta
```

Deberías ver tus ventas.

### PASO 4: Cierra SQL Server y abre Form3 de tu aplicación

**¡Listo! Ahora debería funcionar correctamente.**

---

## ?? Resumen

| Antes | Ahora |
|-------|-------|
| Tabla: "Ventas" (plural) | Tabla: "venta" (singular) ? |
| No funcionaba | Funcionará ? |

---

## ? Ya está arreglado

El código de tu aplicación ya estaba correcto. Solo faltaba crear el procedimiento con el nombre correcto de la tabla.
