# ?? ERROR: "El nombre de objeto 'Ventas' no es válido"

## ¿Qué significa?

La tabla de ventas en tu BD **no se llama 'Ventas'**. Tiene un nombre diferente.

---

## ?? PASO 1: Verifica el nombre de tu tabla

En **SQL Server Management Studio**, ejecuta esto:

```sql
SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_TYPE='BASE TABLE' 
ORDER BY TABLE_NAME
```

Verás una lista de todas tus tablas. Busca la que contiene ventas.

Probablemente se llama:
- `venta` (singular, minúscula) ? **MÁS PROBABLE**
- `Ventas` (plural, mayúscula)
- `VENTAS` (todo mayúscula)
- Otro nombre

---

## ?? PASO 2: Comunícame el nombre

Dime:
1. **Nombre exacto de la tabla de ventas** (cópialo tal como aparece)
2. **Nombre exacto de la tabla de clientes** (cópialo tal como aparece)
3. **Nombres exactos de las columnas** si son diferentes

---

## ?? PASO 3: Yo actualizo el código

Una vez que me des los nombres, actualizaré:
1. El procedimiento SQL
2. El código de VentasDAL.cs
3. Form3.cs

---

## ? SOLUCIÓN RÁPIDA (Si no quieres esperar)

Si sospechas que la tabla se llama `venta` (singular):

Ejecuta esto en SQL Server:

```sql
IF EXISTS (SELECT 1 FROM sys.objects WHERE type = 'P' AND name = 'sp_listar_venta')
    DROP PROCEDURE sp_listar_venta
GO

CREATE PROCEDURE sp_listar_venta
AS
BEGIN
    SELECT 
        ID_venta,
        Fecha_venta,
        ID_cliente,
        Total_general,
        Estado_venta
    FROM venta  -- ? SINGULAR, MINÚSCULA
    ORDER BY Fecha_venta DESC
END
GO
```

Luego intenta abrir Form3 de nuevo.

---

## ?? Ejemplo

Si en SQL ves estas tablas:
```
cliente
detalle_venta
producto
categoria
venta      ? Así se llama tu tabla (singular, minúscula)
```

Entonces la tabla se llama `venta` (no `Ventas`).

---

**Dime el nombre exacto de tu tabla y lo arreglamos en 2 minutos.**
